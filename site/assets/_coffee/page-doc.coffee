
$ ->
  $page = $('.page-doc')

  # $page.on 'mousedown', 'dt', (e) ->
  #   $dt = $(e.currentTarget)
  #   $dd = $dt.next()

  #   expanded = $dd.hasClass 'expand'
  #   $dd.toggleClass 'expand', !expanded
  #   $dt.find('.icon')
  #     .toggleClass('fa-caret-down', !expanded)
  #     .toggleClass('fa-caret-right', expanded)

